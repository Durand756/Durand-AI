# Requirements pour Render GRATUIT (512MB RAM max)
# Version ultra-légère, pas de PyTorch/TensorFlow !

# Framework web minimaliste
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0

# Traitement d'images léger
Pillow==10.0.1
numpy==1.24.4

# Requests pour APIs
requests==2.31.0

# OpenAI (léger)
openai==1.3.7

# Pas de replicate package (on utilise l'API REST directement)
# Pas de torch/diffusers (trop lourd pour Render gratuit)
# Pas de transformers (512MB RAM insuffisante)

# Utilitaires légers seulement
python-dotenv==1.0.0

# C'est tout ! Render gratuit = minimaliste obligatoire
